import {StyleSheet} from 'react-native';
import {INPUT_DEFAULT_HEIGHT} from 'shared-components/Input/InputStyles';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  firstItem: {
    paddingTop: INPUT_DEFAULT_HEIGHT,
  },
});
